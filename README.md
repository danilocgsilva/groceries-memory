To connect to the database, ensure the following environment variables:

* DB_HOST
* DB_NAME
* DB_USER
* DB_PASSWORD
* DB_PORT (optional, asume 3306 if ommited)
* DB_NAME_TEST (optional, only if run tests suites)
